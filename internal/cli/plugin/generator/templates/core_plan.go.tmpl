// GENERATED BY 'T'ool 'G'ateway. DO NOT EDIT.
package core

import "time"

// ExecutionPlanKey - ключ для хранения плана выполнения в request.
const ExecutionPlanKey = "_execution_plan"

// ExecutionPlan содержит план выполнения плагинов.
type ExecutionPlan struct {
	// Current - информация о текущем выполняемом шаге.
	Current StepInfo `json:"current"`

	// Steps - все последовательные шаги выполнения в порядке выполнения.
	// Последний шаг всегда является командой.
	// Порядок выполнения определяется зависимостями: если одна команда зависит от другой,
	// то та команда от которой она зависит выполняется перед ней.
	Steps []StepInfo `json:"steps"`

	// CommandPath - путь команды, которая была вызвана.
	CommandPath []string `json:"commandPath"`

	// CommandArgs - аргументы команды запуска (то же, что и CommandPath).
	CommandArgs []string `json:"commandArgs"`
}

// StepInfo описывает один шаг выполнения.
type StepInfo struct {
	// PluginName - имя плагина для выполнения.
	PluginName string `json:"pluginName"`

	// StepType - тип шага: "transformer" или "command".
	StepType string `json:"stepType"`

	// Version - версия плагина (только для текущего шага).
	Version string `json:"version,omitempty"`

	// RequestKeys - список ключей полей запроса (только для текущего шага).
	RequestKeys []string `json:"requestKeys,omitempty"`

	// ResponseKeys - список ключей полей ответа (только для текущего шага).
	ResponseKeys []string `json:"responseKeys,omitempty"`

	// Duration - время работы плагина (только для текущего шага).
	Duration time.Duration `json:"duration,omitempty"`
}
